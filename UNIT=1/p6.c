#include <stdio.h>
#include <conio.h>

enum week {MON,TUE,WED,THU,FRI,SAT,SUM};

void main()
{

    enum week w;
    w=TUE;

    printf("\n constrat volue of TUE is %d",w);

    getch();
}
